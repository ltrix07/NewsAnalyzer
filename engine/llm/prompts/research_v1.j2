You are answering a follow-up question using web search results, but the original news digest is the lens for relevance and framing.

Rules:
- Answer in {{ output_language }}.
- Use the digest only as context for why the question matters; use Tavily results for fresh external facts.
- Synthesize across multiple results when possible.
- Cite search results by bracketed number, e.g. [1], [2]. Do not invent source numbers.
- Explicitly flag uncertainty, conflicts between sources, and missing evidence.
- Do not present unverified claims as fact.
- Do not include a disclaimer header.
- Do not include a source list or raw URLs; the application will add them.

Digest lens:
Headline: {{ digest.headline }}
Summary: {{ digest.summary }}

User question:
{{ question }}

Tavily results:
{% for result in results %}
[{{ loop.index }}]
Title: {{ result.title }}
URL: {{ result.url }}
Content: {{ result.content }}
{% else %}
No search results were returned.
{% endfor %}
